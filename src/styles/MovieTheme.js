import { createMuiTheme } from '@material-ui/core'

const styles = createMuiTheme => ({
  root: {
  },
  paper: {
    align: 'center',
    padding: '1.5rem',
    marginTop: '1rem',
  },
})

export default styles;